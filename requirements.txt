# NOTA: el proyecto usa ahora pyproject.toml + uv como fuente principal de
# dependencias (ver README, sección 0). Este archivo se mantiene solo como
# referencia/fallback por si prefieres pip clásico; puedes regenerarlo con:
#   uv export --no-hashes -o requirements.txt

# Parseo de correo
mail-parser>=4.0.1
beautifulsoup4>=4.12

# Extracción de adjuntos
unstructured[pdf,docx,pptx,xlsx]>=0.15
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
pypdf>=4.0

# Embeddings + vector DB
ollama>=0.3.0
qdrant-client>=1.9

# RAG / orquestación
llama-index>=0.11
llama-index-vector-stores-qdrant>=0.3
llama-index-embeddings-ollama>=0.3
llama-index-llms-ollama>=0.3

# Grafo (fase 2)
neo4j>=5.20

# Utilidades
tqdm>=4.66
python-dateutil>=2.9
